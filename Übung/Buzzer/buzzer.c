#include <wiringPi.h>
#include <stdio.h>
#include <unistd.h> // Für sleep-Funktion
#include "buzzer.h" // Inkludiert den Header für den Buzzer

void initializeBuzzer(void) {
    //
}

void activateBuzzer(int duration) {
    // Buzzer an
    // Warte für die angegebene Dauer
    // Buzzer aus
}

int main(void) {
    //Set up wiring Pi und check ob -1 return wird -> fehler

    //init Buzzer
    // Aktiviert den Buzzer für 1 Sekunden

    return 0; // Beendet das Programm erfolgreich
}
